NAME
    cube - spinning wireframe 3D cube (the stage-7 graphics demo)

SYNOPSIS
    cube [frames] [s]

DESCRIPTION
    Draws a perspective wireframe cube on the display and spins it about
    two axes, one frame at a time, then exits. With no argument it runs 64
    frames (one full turn); a numeric argument runs that many. Needs the
    display; prints ?No display without one.

    The cube is the first client of the C 3D library (/lib/lib_g3d.c, used
    as `//#use gfx` + `//#use g3d` from C): a retained pool of 3D edges
    (g3line), a window on the view plane (g3window), a viewport on the
    screen (g3view), perspective projection (g3persp), and a render call
    (g3render) that clips, projects, maps and hands each surviving edge to
    the display's hardware LINE. The rotation itself is the demo's own
    integer sine table, not the library's.

    With the geometry engine fitted (stage 8b) the cube is uploaded to the
    engine once and each frame is a matrix write plus one render command:
    the CPU does ~3 ms of work, the fabric does the rest, and the page
    flip paces the spin to the panel's refresh, tear-free. On the way in
    it clears BOTH framebuffer pages (their power-on contents are
    undefined -- raw memory), and on the way out it rejoins the draw and
    display pages, so drawing afterwards behaves normally. `cube N s`
    forces the stage-7 all-software path (~72 ms a frame) for comparison;
    without an engine that path is the automatic fallback. Either way the
    cube is rebuilt from constants every frame, so it never accumulates
    rounding error and frame N looks the same on every run.

EXAMPLES
    cube            one full turn and exit
    cube 500        keep spinning for 500 frames

SEE ALSO
    g3d, gfx, basic (GRAPHICS section)
