.DS_Store
# Eagle auto-backups
*.s#?
*.b#?
# Fusion exports / fab outputs (regenerate, don't track)
gerbers/
*.zip
# Build / test artifacts (regenerate with `make`)
emulator/p8xemu
emulator/*.o
emulator/test/*.bin
emulator/test/*.out
emulator/test/cf.img
emulator/test/os.img
emulator/test/v2.img
emulator/test/bas.img
emulator/test/tinyos.asm
emulator/test/prog.asm
emulator/test/v2prog.asm
emulator/test/*.tmp
# combined monitor+BASIC ROM (rebuild with tools/build_basic_rom.py)
/p8x-rom-basic.bin
# stray microcode images if a generator is run from the repo root
/u?.bin
/os/run-disk.img
emulator/test/*.hex
/u?.hex
/p8x-rom-basic.hex
microcode/u?.hex
emulator/test/io.asm
emulator/u?.bin
emulator/test/v2bad.img

# Eagle/ libraries + design rules ARE tracked (so the project is self-contained
# for anyone reproducing it). Skip only the redundant pre-conversion binary backup.
Eagle/Libraries/*.orig.lbr
