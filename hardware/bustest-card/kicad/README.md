# bustest-card — KiCad (PLACED, not fully routed)

Generated by `generators/gen_kicad.py` from the canonical gen_eagle netlist
(`CARDS["bustest-card"]`): 4-layer (In1=GND, In2=VCC planes), a bypass cap above
each IC, part values on silk, bus connector on the left.

**Status: placed + netlisted + planes, NOT fully autorouted.** This card carries a
Raspberry Pi Pico (substituted as a 2×20 header) whose pins fan into most of the
bus, which made the 2-signal-layer autoroute too dense for Freerouting to finish
in reasonable time. The board, footprints, full netlist and power planes are all
in place; completing the route (a denser hand placement or a longer Freerouting
pass) is the remaining step. GENERATORS ARE CANON — edit `gen_kicad.py` and re-run
`generators/build_kicad_card.sh bustest-card`.

Exotic parts use the closest standard KiCad footprint (Pico→2×20 header, isolated
R-networks→DIP-16), flagged when built.
